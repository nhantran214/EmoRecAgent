# RecBole-TiSASRec baseline (AC-TSR fork + EmoRecAgent processed splits)
# Install: pip install -r baseline/RecBole-TiSASRec/requirements.txt
#
# Uses the vendored AC-TSR RecBole package under vendor/ (TiSASRec + CE).
# The PyPI `recbole` package is optional; vendor takes PYTHONPATH precedence.

torch>=2.0
numpy>=1.19
PyYAML>=6.0
tqdm>=4.66
einops>=0.6
colorlog>=4.0
colorama>=0.4
scikit-learn>=1.0
scipy>=1.7
pandas>=1.3
tabulate>=0.8
plotly>=4.0
tensorboard>=2.5
thop>=0.1
