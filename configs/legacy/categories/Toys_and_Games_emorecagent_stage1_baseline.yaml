# Protocol B Stage-1-only baseline for Toys and Games (same method as Beauty).
extends: Toys_and_Games_era_base.yaml

experiment:
  name: emorecagent_stage1_baseline_Toys_and_Games

eval:
  protocol: user_batch
  aggregation: user_mean
  k_values: [10, 20]
  hr_avg_k: [1, 3, 5]

tisasrec_align:
  use_hash_encoder: true
  stage1_only: true
