# Archived configs (not the default pipeline)

Prefer `configs/categories/{Category}.yaml` (RecBole CE Stage-1 + φ LTR +
listwise LLM Stage-2). This tree keeps older overlays and paper-split recipes.
