# Protocol B Stage-1-only baseline for Yelp (same method as Beauty / Sports / Toys).
extends: Yelp.yaml

experiment:
  name: emorecagent_stage1_baseline_Yelp

eval:
  protocol: user_batch
  aggregation: user_mean
  k_values: [10, 20]
  hr_avg_k: [1, 3, 5]

tisasrec_align:
  use_hash_encoder: true
  stage1_only: true
